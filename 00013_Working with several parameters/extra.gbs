procedure Drop(color){
  PutStone(color)
}

procedure Grab(color){
  RemoveStone(color)
}

procedure ComeBack(){
  Move(West)
  Move(West)
}

procedure DrawLine3(color, direction){
  Drop(color)
  Move(direction)
  Drop(color)
  Move(direction)
  Drop(color)
}
