procedure Drop(color){
  PutStone(color)
}

procedure Grab(color){
  RemoveStone(color)
}

procedure DrawLine3(color, direction){
  Drop(color)
  Move(direction)
  Drop(color)
  Move(direction)
  Drop(color)
  ComeBack()
}

procedure ComeBack(){
  Move(West)
  Move(West)
}